[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "canal-delights"
version = "0.1.0"
description = "An interactive Grand Canal food scroll made with Python Turtle"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
